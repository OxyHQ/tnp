FROM oven/bun:1.3.11
WORKDIR /app

COPY package.json bun.lock ./
COPY apps/relay/package.json apps/relay/
RUN bun install --production

COPY apps/relay/src apps/relay/src

ENV NODE_ENV=production
ENV RELAY_PORT=8080
EXPOSE 8080

CMD ["bun", "apps/relay/src/index.ts"]
